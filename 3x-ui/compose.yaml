services:
  3x-ui:
    image: ghcr.io/mhsanaei/3x-ui:v2.4.5
    container_name: 3x-ui
    hostname: SERVER-HOSTNAME
    network_mode: host
    tty: false
    volumes:
      - $PWD/3x-ui/db/:/etc/x-ui/
      - $PWD/certs/:/etc/letsencrypt/:rw
    environment:
      - XRAY_VMESS_AEAD_FORCED=false
